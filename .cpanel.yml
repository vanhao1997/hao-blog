---
deployment:
  tasks:
    - export DEPLOYPATH=/home/qskqmzfd/public_html/
    - /bin/cp -R about.html blog.html contact.html index.html post.html 404.html $DEPLOYPATH
    - /bin/cp -R .htaccess $DEPLOYPATH
    - /bin/cp -R css/ $DEPLOYPATH
    - /bin/cp -R js/ $DEPLOYPATH
    - /bin/cp -R images/ $DEPLOYPATH
    - /bin/cp -R api/ $DEPLOYPATH
    - /bin/cp -R admin/ $DEPLOYPATH
    - /bin/cp -R tool/ $DEPLOYPATH
    - /bin/cp -R toolkit/ $DEPLOYPATH
    - /bin/cp -R uploads/ $DEPLOYPATH
    - /bin/cp -R partials/ $DEPLOYPATH
    - /bin/cp manifest.json robots.txt sitemap.xml sw.js $DEPLOYPATH 2>/dev/null; true
    - if [ -d "$DEPLOYPATH/api_test" ]; then rm -rf "$DEPLOYPATH/api_test"; fi
    - rm -f "$DEPLOYPATH/api/.env" 2>/dev/null; true

